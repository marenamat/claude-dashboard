date: '2026-04-06'
branch: issue-2-separate-nginx

summary: |
  Maintenance + CI fix session.

  Status:
  - issue-1 (base implementation): complete, code merged to local main
  - issue-2 (separate nginx): complete; nginx/clanker.conf, nginx/proxy.conf,
    etc/init.d/claude-dashboard all in place
  - Both issues awaiting push to remote by human guardian

  Pipeline failures (all pre-2026-04-06):
  - All 7 known failures are from old wasm-pack CI workflow
  - Current code replaced wasm-pack with cargo + wasm-bindgen-cli (commit 7d5b1e8)
  - CI fix further improved at commit 877ff43 (lockfile + exact version matching)
  - Neither fix has been pushed to remote, so no new CI run has been triggered

  Git status:
  - local main diverges from remote main (clanker-prep ff failed)
  - issue-1-base-implementation and issue-2-separate-nginx are up-to-date locally
  - issue-2-separate-nginx branch contains all work including CI fix

  Twelfth maintenance run (2026-04-06):
  - No new issues, no code changes needed
  - Context timestamps updated by clanker-prep
