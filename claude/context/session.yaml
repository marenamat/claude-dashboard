recorded_at: '2026-04-17T12:00:00Z'

ci_status:
  main: Package CI was failing (Build Alpine APK step) — fixed in issue-14-nginx branch
  root_cause: APK version used git short SHA (hex chars invalid for APK version format)
  fix: use git rev-list --count HEAD (pure decimal) — in issue-14-nginx branch
  note: issue-14-nginx branch not yet merged to main

issues_pending_review:
  - 14  # nginx packaging + CI version fix (branch: issue-14-nginx)
  - 19  # relative date format (branch: issue-19-dates)
  - 10  # auto-approval badge in log overlay (branch: issue-10-log-overlay, this branch)
  - 12  # offending action in denied permissions (branch: issue-12-denied-action)

issues_implemented_in_main_not_yet_closed:
  - 18  # raise log processing limit (08fc3f1 in main)
  - 17  # collapse no-work entries (911c894 in main)
  - 16  # show clone commands (47ead3b in main)
  - 4   # layout compact (1a43fce in main): col-12 col-md-6 col-xxl-4

issues_blocked:
  - 9   # autoreload (needs py3-websockets installed + nginx setup, see questions.md)

session_2026-04-17b:
  - Package CI root cause: git short SHA contains hex chars invalid for APK versions.
    Fixed in issue-14-nginx branch.
  - Issue #10 additional comment (4266019082):
    Added "approved by config" green badge on tool_use entries in log overlay.
    Two-pass: first scan user messages for tool_use_id→approval, then render.
    This branch: issue-10-log-overlay (commit 7c3cf8d).
  - Issue #12 additional comment (4263489021):
    _parse_denial extracts clean command/path; overlay shows action in
    red-bordered code block. Branch: issue-12-denied-action (commit 21dec0c).
  - issue-17-collapse-no-work rebase failed — feature already in main (911c894).
    Branch can be deleted by guardian.
