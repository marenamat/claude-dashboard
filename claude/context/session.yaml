recorded_at: '2026-04-17T17:00:00Z'

ci_status:
  main: Package CI still failing (Build Alpine APK step, ~3 seconds) even after
        version format fix merged to main. Diagnostics branch issue-14-ci-debug
        adds set -x, pre-flight checks, and hardens abuild-keygen (removed -i flag,
        manual pub key install, handles .rsa/.pem extensions). Root cause unknown
        without actual log access (requires admin auth).

issues_pending_review:
  - 10  # log overlay: collapse raw-JSON dumps (branch: issue-10-raw-json)
  - 14  # Package CI diagnostics + abuild-keygen hardening (branch: issue-14-ci-debug)

issues_in_main_not_yet_closed:
  - 4   # layout compact: col-12 col-md-6 col-xxl-4 — 1a43fce
  - 9   # autoreload (partially in main via ws-server.py, but blocked on deps)
  - 10  # log overlay improvements: most sub-tasks merged, raw-JSON collapse pending
  - 12  # denied permissions overlay (merged to main)
  - 16  # show clone commands overlay (merged to main)
  - 17  # collapse no-work/limit-hit entries (merged to main)
  - 18  # raise log processing limit to 10 days (merged to main)
  - 19  # relative date format + DOW (merged to main)

issues_blocked:
  - 9   # autoreload websocket server — needs py3-websockets package, see questions.md
  - 15  # spawner — needs crontab + config.yaml spawner section, see questions.md

branches_stale:
  - issue-4-layout-compact  # feature in main; no unique commits; safe to delete

session_2026-04-17g:
  - Package CI: "Build Alpine APK" fails in ~3 seconds consistently; version format
    fix (git rev-list --count) already in main since 11:11 UTC but CI still failing.
    Most recent failing run (24566073978) started at 12:53:51Z and completed at
    12:53:54Z — only 3 seconds, implies failure before or immediately at Docker start.
  - Issue #10 comment 4268155381 ("ok collapse the raw-JSON dumps and keep the known
    uncollapsed"): implemented in branch issue-10-raw-json. Unknown record types and
    unknown content blocks now use buildCollapsibleBlock() instead of bare <pre>.
  - Package CI: created branch issue-14-ci-debug with set -x + docker version check
    + explicit tarball verification + hardened abuild-keygen (no -i, manual pub key
    copy, handles .rsa/.pem).
  - Dropped stash (changes captured in their respective branches).
