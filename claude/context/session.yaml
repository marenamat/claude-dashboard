recorded_at: '2026-04-19T20:30:00Z'

ci_status:
  main: All pipelines green (Build, Package, Deploy). Latest run at 2026-04-19T20:16Z.

issues_pending_review: []

issues_in_main:
  - 9   # WS autoreload code in main; packaging/nginx fixed; guardian still needs to deploy
  - 16  # Clone commands fix (static HTML + WASM &#10; encoding) in main; guardian needs to deploy
  - 21  # spawn from GitHub: merged
  - 22  # extract tool_input from permission_denials: merged

issues_blocked:
  - 15  # spawner — needs crontab + config.yaml spawner section, see questions.md
  - 9   # guardian still needs to enable claude-dashboard-ws service and copy nginx snippet

questions_open:
  - issue-15: crontab + spawner section in config.yaml
  - issue-9: guardian must copy nginx conf and enable claude-dashboard-ws service

session_2026-04-19c:
  - Workflow check: pipelines all green, no new issues to work on.
  - Issue #9: code/packaging fixes all merged. Guardian needs to install latest APK,
    copy /usr/share/claude-dashboard/nginx/clanker.conf → /etc/nginx/http.d/,
    reload nginx, then enable+start claude-dashboard-ws.
  - Issue #16: both fixes merged (68fa837 static HTML &#10; encoding, 15c2b41 WASM
    &#10; encoding). Guardian's "only clone command" comment predates these fixes.
    Guardian needs to install latest APK to verify fix.
  - No design/implementation discrepancies found.
  - questions.md unchanged: both open items still awaiting guardian action.
  - issue-16-wasm-clone-newlines branch merged+pruned; only issue-9-cron-fix
    (identical to main) and main remain locally.

session_2026-04-19b:
  - Pipelines: main all green as of 2026-04-19T19:52Z. Latest run (2026-04-19T19:58Z)
    still in-progress at context-fetch time (includes claude-base template merge).
  - issue #16: Found root cause of guardian's "only clone command" report. The static
    HTML generator (generate-data.py) had the &#10; fix since 68fa837, but the Rust/WASM
    renderer (src/lib.rs) did NOT. After any WebSocket-triggered live-reload, the WASM
    regenerates HTML using esc() which skips newlines; browser normalises them to spaces;
    only one merged line shows in overlay. Fixed: added .replace('\n', "&#10;") in
    render_project_card() after esc() call. Branch: issue-16-wasm-clone-newlines.
  - issues_pending_review: #16 (issue-16-wasm-clone-newlines)
  - issues_blocked: #9 (guardian still needs to enable ws service + nginx conf),
      #15 (guardian needs crontab + spawner config)
  - questions.md open items unchanged (guardian action required)

session_2026-04-19a:
  - Pipeline: main Build+Deploy+Package still in-progress (triggered 2026-04-19T16:03).
  - issue-16-fix-clone-newlines: rebase was failing (session.yaml conflict). Resolved
    by keeping HEAD/main session.yaml and rebasing the generate-data.py fix cleanly.
    Branch now cleanly ahead of main with one commit (f5dc708). Pending guardian merge.
  - No new issues. questions.md open items still pending guardian action (issues #9, #15).
  - issues_pending_review: #16 (issue-16-fix-clone-newlines); #22 merged into main already

session_2026-04-18i:
  - Rebase of issue-22-denied-permissions onto main completed successfully.
    Conflicts resolved: d418c4a (stale dependency doc — kept HEAD/main version),
    0ced3a3 (removed issue-22 section from questions.md as resolved).
  - issue-9-cron-fix branch pruned (all commits merged into main; WS works per guardian).
  - questions.md cleaned up: removed stale WS diagnostic questions block (WS works now).
  - Pipelines all green on main.
  - Pending guardian merges: issue-16-fix-clone-newlines, issue-22-denied-permissions.

session_2026-04-18h:
  - issue #22 fix: Claude Code stream-json result record includes permission_denials
    with full tool_input dict (e.g. {"command": "ls /foo"}), but _parse_denial() was
    looking for "input" key not "tool_input". Fixed by adding d.get("tool_input")
    to the lookup chain. The data was always there; just not being extracted.
    Removed the outdated "dependency on clanker-run" section from questions.md.
  - issues_pending_review: #16, #22 (generate-data.py fix)
  - issues_blocked: #9 (guardian needs to install new APK, enable ws service),
      #15 (guardian needs crontab + config.yaml spawner section)
